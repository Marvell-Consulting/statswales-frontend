export enum DimensionType {
    Raw = 'raw',
    Text = 'text',
    Numeric = 'numeric',
    Symbol = 'symbol',
    ReferenceData = 'reference_data',
    LookupTable = 'lookup_table',
    DatePeriod = 'date_period',
    Date = 'date',
    FootNote = 'footnote',
    Measure = 'measure'
}
