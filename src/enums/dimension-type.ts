export enum DimensionType {
    Raw = 'raw',
    Text = 'text',
    Numeric = 'numeric',
    Symbol = 'symbol',
    LookupTable = 'lookup_table',
    TimePeriod = 'time_period',
    TimePoint = 'time_point',
    FootNote = 'footnote'
}
