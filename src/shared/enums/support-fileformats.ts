export enum DuckDBSupportFileFormats {
  csv = '.csv, text/csv',
  parquet = '.parquet,application/vnd.apache.parquet, application/parquet',
  json = '.json,application/json',
  xlsx = '.xlsx, application/vnd.ms-excel,application, vnd.openxmlformats-officedocument.spreadsheetml.sheet, application/msexcel',
  gz = '.gz, application/gzip'
}
